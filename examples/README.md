# Examples

Sample repositories you can use as starting points:

- `examples/personal`: personal knowledge management (journal, tasks, contacts)
- `examples/consulting`: client/project/invoice workflow for a solo consultancy

Use any example via `-C`:

```bash
bun run src/main.ts -C examples/personal list -o table
bun run src/main.ts -C examples/consulting list -o table
```
