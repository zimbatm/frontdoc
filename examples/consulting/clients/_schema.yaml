fields:
  name:
    type: string
    required: true
  email:
    type: email
  website:
    type: url
  active:
    type: boolean
  status:
    type: enum
    enum_values:
      - lead
      - active
      - paused
